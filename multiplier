//This doesn't use Simusense's own parsing capacities yet

function multiplier(a,b) {
var multiplicator = [];
for (i=1;i<=a;i++) {
for (j=1;j<=b;j++) {
multiplicator.push(1);
}
}
return multiplicator.reduce(function addUp(c,d){
return c+=d;
});
}

multiplier(23,95)
