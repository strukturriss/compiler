This is the JS-based simulator for the 'multiplier' part of the compiler's multiplication engine, for testing:

function multiplier(a,b) {
var multiplicator = [];
for (i=1;i<=a;i++) {
for (j=1;j<=b;j++) {
multiplicator.push(1);
}
}
return multiplicator.reduce(function addUp(c,d){
return c+=d;
});
}

multiplier(23,95)

The blueprint there is on p. 9 of the notebook: this one uses nothing but WRITE, INCREMENT, IF...THEN...ELSE, and GOTO.
